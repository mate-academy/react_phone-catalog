export interface ProductSpecs {
  description: string;
  name: string;
  images: string[];
  id: string;
  camera: string;
  screen: string;
  resolution: string;
  processor: string;
  ram: string;
  zoom: string;
  cell: string[];
  capacity: string;
}
