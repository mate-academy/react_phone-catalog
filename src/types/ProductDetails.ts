export type ProductDetails = {
  capacity: string[];
  colors: Color[];
  processor: string;
  ram: string;
  screen: string;
  resolution: string;
  camera: string;
  zoom: string;
  cell: string;
};

export type Color = {
  color: string;
  hex: string;
};
