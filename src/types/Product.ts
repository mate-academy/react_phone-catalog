export interface Product {
  id: string;
  category: string;
  itemId: string;
  name: string;
  image: string;
  price: number;
  fullPrice?: number;
  screen: string;
  capacity: string;
  ram: string;
  quantity: number;
}
