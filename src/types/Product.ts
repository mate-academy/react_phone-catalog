import { Category } from './Category';

export interface Product {
  capacity: string,
  category: Category,
  color: string,
  fullPrice: number,
  id: string,
  image: string,
  itemId: string,
  name: string,
  phoneId: string,
  price: number,
  ram: string,
  screen: string,
  year: number,
}
