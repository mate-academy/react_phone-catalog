import { Category } from './Category';

export enum BreadcrumbsT {
  PHONES = Category.PHONES,
  TABLETS = Category.TABLETS,
  ACCESSORIES = Category.ACCESSORIES,
  FAVOURITES = 'favourites',
  CART = 'cart',
}
