import React from 'react';
import { Link } from 'react-router-dom';

export const Cart = () => {
  return (
    <div className="cart">
      <Link to="/" className="cart__button" />
    </div>
  );
};
