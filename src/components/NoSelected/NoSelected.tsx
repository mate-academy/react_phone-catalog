import React from 'react';
import './NoSelected.scss';

export const NoSelected = () => (
  <div className="NoSelected">
    <h1 className="NoSelected-Title">
      There is no selected products...
    </h1>
  </div>
);
