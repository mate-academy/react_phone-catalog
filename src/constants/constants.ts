import {
  banner,
  bannerAccessories,
  bannerPhones,
  bannerTablets,
} from '../assets/index';

export const TIME_SLIDER = 5000;

export const slideImages = [
  banner,
  bannerPhones,
  bannerTablets,
  bannerAccessories,
];
