# Internationalization (i18n)

Проект підтримує мультимовність через **react-i18next**.

## Підтримувані мови

- 🇬🇧 **English (en)** - англійська (за замовчуванням)
- 🇺🇦 **Ukrainian (uk)** - українська

## Використання

### В компонентах

```tsx
import { useTranslation } from 'react-i18next';

const MyComponent = () => {
  const { t } = useTranslation();

  return <h1>{t('nav.home')}</h1>;
};
```

### З параметрами

```tsx
// Простий параметр
t('category.searchIn', { category: 'phones' });
// => "Search in phones..."

// Множина
t('favorites.items', { count: 5 });
// => "5 items"
```

## Структура перекладів

```
i18n/
  └── i18n.ts - конфігурація з усіма перекладами
```

### Категорії перекладів:

- **nav** - навігація (Home, Phones, Tablets, Accessories)
- **header** - хедер (cart, favorites, menu)
- **common** - загальні (back, loading, search)
- **home** - головна сторінка
- **category** - сторінка категорії
- **favorites** - сторінка обраного
- **cart** - кошик
- **product** - деталі товару
- **productCard** - картка товару

## Компонент LanguageSwitcher

Перемикач мов в хедері:

- EN / UA кнопки
- Активна мова підсвічується
- Вибір зберігається в localStorage

```tsx
<LanguageSwitcher />
```

## Збереження вибору

Мова зберігається в `localStorage` під ключем `'language'` і відновлюється при наступному візиті.

## Додавання нових перекладів

1. Відкрийте `src/i18n/i18n.ts`
2. Додайте переклад в обидва об'єкти (`en` та `uk`)
3. Використовуйте в компоненті через `t('ключ')`

```typescript
const resources = {
  en: {
    translation: {
      myNew: {
        key: 'English text',
      },
    },
  },
  uk: {
    translation: {
      myNew: {
        key: 'Український текст',
      },
    },
  },
};
```
