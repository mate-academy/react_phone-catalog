import styles from './PicturesSlider.module.scss';

export const PicturesSlider = () => {
  return (
    <section className={styles.slider}>
      <div className={styles.placeholder}>Pictures slider</div>
    </section>
  );
};
