import { ProductsStyleMode } from '../types/types';

export const ProductsStyleTitleMap: Record<ProductsStyleMode, string> = {
  [ProductsStyleMode.New]: 'Brand new models',
  [ProductsStyleMode.Hot]: 'Hot prices',
  [ProductsStyleMode.Also]: 'You may also like',
};
