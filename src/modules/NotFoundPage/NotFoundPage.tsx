import { BackBtn } from '../../components/BackBtn';

export const NotFoundPage = () => (
  <div className="not-found page">
    <div className="container">
      <BackBtn />
      <h1 className="page-title">NotFoundPage</h1>
    </div>
  </div>
);
