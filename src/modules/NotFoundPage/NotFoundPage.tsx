import React from 'react';
import { Header } from '../../components/Header';

export const NotFoundPage: React.FC = () => {

    return (

      <div>
        <Header />
        <center>
          In progress
        </center>
      </div>
    );
};
