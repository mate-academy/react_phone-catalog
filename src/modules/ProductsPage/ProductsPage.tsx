// import { useLocation } from 'react-router-dom';

import './ProductsPage.scss';

export const ProductsPage = () => {
  // const { pathname } = useLocation();
  // const category = pathname.split('/').at(-1) as string;

  return (
    <div className="home-page">
      <h1>HomePage</h1>
    </div>
  );
};
