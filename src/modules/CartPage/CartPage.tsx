import React from 'react';
import { Header } from '../../components/Header';

export const CartPage: React.FC = () => {

    return (

      <div>
        <Header />
        <center>
          In progress
        </center>
      </div>
    );
};
