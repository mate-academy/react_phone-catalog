import React from 'react';
import CategoryPage from '../../shared/components/CategoryPage';

export const TabletsPage: React.FC = () => {
  return <CategoryPage type={'tablets'} />;
};

export default TabletsPage;
