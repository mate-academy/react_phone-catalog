import { Page, Section } from '@/atoms';

const Tablets = () => {
  return (
    <Page>
      <Page.Breadcrumps />
      <Section>
        <Section.Title>Tablets</Section.Title>
      </Section>
    </Page>
  );
};

export default Tablets;
