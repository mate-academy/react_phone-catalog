import { NotFound } from '../../components/NotFound';

export const NotFoundPage = () => (
  <div className="notFoundPage">
    <NotFound title={'Page not found'} imgSrc={'page-not-found.png'} />
  </div>
);
