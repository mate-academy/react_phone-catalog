import { Mode } from '@widgets/Slider/model/defaultConfig';

export const heroSliderConfig = {
  mode: Mode.INFINITE,
  snap: true,
};

export const prodSliderConfig = {
  mode: Mode.CLAMP,
  snap: true,
  gap: 30,
};
